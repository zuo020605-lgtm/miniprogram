/* ============================================
   首页样式 - 新设计规范
   ============================================ */

page {
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 100%);
}

.home-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 100%);
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}

/* ========== 全局文字色 ========== */
.text-primary {
  color: #3b82f6;
}

.text-secondary {
  color: #6b7280;
}

.text-tertiary {
  color: #9ca3af;
}

/* ========== 动画效果 ========== */
@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* ========== 深色模式适配 ========== */
@media (prefers-color-scheme: dark) {
  .page-container {
    background: linear-gradient(180deg, #0f172a 0%, #1e293b 100%);
  }
  
  .text-primary {
    color: #60a5fa;
  }
  
  .text-secondary {
    color: #94a3b8;
  }
  
  .text-tertiary {
    color: #cbd5e1;
  }
  
  .header {
    background: rgba(15, 23, 42, 0.85);
  }
  
  .search-input {
    background-color: #1e293b;
    color: #f8fafc;
  }
  
  .search-input::placeholder {
    color: #94a3b8;
  }
  
  .task-card,
  .post-item {
    background-color: #1e293b;
    box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.15);
  }
  
  .task-title,
  .post-title {
    color: #f8fafc;
  }
  
  .meta-text,
  .post-meta .meta-item {
    color: #94a3b8;
  }
}

/* ========== 顶部导航 ========== */
.header {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  padding-top: calc(var(--status-bar-height, 44rpx) + 24rpx);
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.05);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

/* [修复] 补充缺失的 glass-header 类定义（WXML 中使用但 CSS 中未定义） */
.glass-header {
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.campus-selector {
  display: flex;
  align-items: center;
  gap: 16rpx;
  /* iOS 风格动画 */
  transition: opacity 0.1s ease;
}

.campus-selector:active {
  opacity: 0.6;
}

.icon-school {
  width: 48rpx;
  height: 48rpx;
}

.campus-info {
  display: flex;
  flex-direction: column;
}

.campus-label {
  font-size: 20rpx;
  color: #595c5e;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.campus-name-wrapper {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.campus-name {
  font-size: 32rpx;
  font-weight: 700;
  color: #2c2f32;
}

.icon-chevron {
  width: 24rpx;
  height: 24rpx;
  margin-left: 8rpx;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.notification-btn {
  position: relative;
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  /* iOS 风格动画 */
  transition: transform 0.1s cubic-bezier(0.25, 0.46, 0.45, 0.94), opacity 0.1s ease;
}

.notification-btn:active {
  transform: scale(0.85);
  opacity: 0.7;
}

.icon-bell {
  width: 28rpx;
  height: 28rpx;
}

.notification-badge {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  min-width: 32rpx;
  height: 32rpx;
  padding: 0 8rpx;
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%);
  color: #ffffff;
  font-size: 18rpx;
  font-weight: 700;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: pulse 2s infinite;
  box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.4);
}

.user-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(59, 130, 246, 0.3);
  /* iOS 风格动画 */
  transition: transform 0.1s cubic-bezier(0.25, 0.46, 0.45, 0.94), opacity 0.1s ease;
}

.user-avatar:active {
  transform: scale(0.88);
  opacity: 0.8;
}

.avatar-text {
  font-size: 24rpx;
  font-weight: 700;
  color: #edf3ff;
}

/* ========== Hero Section ========== */
.hero-section {
  padding: 48rpx 32rpx 32rpx;
}

.hero-title {
  display: flex;
  flex-direction: column;
  font-size: 48rpx;
  font-weight: 800;
  line-height: 1.2;
  color: #1a1a1a;
  margin-bottom: 32rpx;
  letter-spacing: -0.02em;
}

.search-wrapper {
  position: relative;
  display: flex;
  align-items: center;
  z-index: 20;
}

.search-icon-wrapper {
  position: absolute;
  left: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.search-icon {
  width: 32rpx;
  height: 32rpx;
  transition: opacity 0.2s ease;
}

.search-wrapper:focus-within .search-icon {
  opacity: 1;
}

.search-input {
  width: 100%;
  height: 96rpx;
  padding: 0 24rpx 0 80rpx;
  background-color: #ffffff;
  border: 2rpx solid transparent;
  border-radius: 32rpx;
  font-size: 28rpx;
  color: #1a1a1a;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  transition: all 0.2s ease;
}

.search-input:focus {
  border-color: #3b82f6;
  box-shadow: 0 4rpx 24rpx rgba(59, 130, 246, 0.15);
}

.search-input::placeholder {
  color: #6b7280;
}

.search-history-panel {
  position: relative;
  z-index: 19;
  margin-top: 16rpx;
  padding: 24rpx;
  background: #ffffff;
  border-radius: 24rpx;
  box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.08);
}

.search-history-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 18rpx;
}

.search-history-title {
  font-size: 26rpx;
  font-weight: 800;
  color: #2c2f32;
}

.search-history-clear {
  font-size: 22rpx;
  font-weight: 700;
  color: #005da4;
}

.search-history-list {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.search-history-item {
  max-width: 100%;
  height: 58rpx;
  padding: 0 22rpx;
  border-radius: 29rpx;
  display: flex;
  align-items: center;
  gap: 10rpx;
  background: rgba(0, 93, 164, 0.08);
  color: #005da4;
  box-sizing: border-box;
}

.history-clock {
  font-size: 22rpx;
  flex-shrink: 0;
}

.history-keyword {
  max-width: 520rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 24rpx;
  font-weight: 600;
}

/* ========== Bento Grid ========== */
.function-section {
  padding: 0 32rpx 32rpx;
}

.bento-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: auto auto;
  gap: 24rpx;
}

.bento-item {
  border-radius: 24rpx;
  /* iOS 风格动画 */
  transition: all 0.2s ease;
}

.bento-item:active {
  transform: scale(0.985);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.12);
}

/* 大卡片 - 校园代课 */
.bento-item-large {
  grid-row: span 2;
  padding: 24rpx 32rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  align-self: stretch;
}

.gradient-primary {
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%);
}

.bento-icon-large {
  width: 72rpx;
  height: 72rpx;
  margin-bottom: 16rpx;
}

.bento-info {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.bento-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #edf3ff;
}

.bento-subtitle {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.8);
}

/* 右侧小卡片组 */
/* [核心修复] 补充缺失的 .bento-item-group 样式：
   原来只有裸 <view> 包裹两个小卡片，没有任何 flex/gap 声明，
   导致两张卡片之间完全没有间距。
   同时需要 height: 100% 让它撑满左侧大卡片的高度，
   再用 flex: 1 让两张小卡片均分高度。 */
.bento-item-group {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  height: 100%;
  grid-row: span 2
}

.bento-item-small {
  flex: 1; /* 让两张小卡片均分 .bento-item-group 的高度 */
  padding: 24rpx;
  display: flex;
  align-items: center;
  gap: 20rpx;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  background-color: #ffffff;
}

.bg-secondary-container {
  background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
}

.bg-surface-container-highest {
  background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
}

.icon-wrapper-white {
  width: 72rpx;
  height: 72rpx;
  background-color: #ffffff;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.08);
  flex-shrink: 0;
}

.bento-icon {
  width: 40rpx;
  height: 40rpx;
}

.bento-info-small {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.bento-title-small {
  font-size: 28rpx;
  font-weight: 700;
}

.bento-subtitle-small {
  font-size: 20rpx;
}

.text-on-secondary-container {
  color: #004496;
}

.text-on-secondary-container-70 {
  color: rgba(0, 68, 150, 0.7);
}

.text-on-surface {
  color: #2c2f32;
}

.text-on-surface-variant {
  color: #595c5e;
}

.text-secondary {
  color: #1658b6;
}

/* 全宽卡片 - 考试代替 */
/* [修复] 原来 .bento-item-full 定义了三次，合并为一次 */
.bento-item-full {
  grid-column: span 2;
  padding: 16rpx 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  border-radius: 24rpx;
  /* iOS 风格动画 */
  transition: all 0.2s ease;
}

.bento-item-full:active {
  transform: scale(0.985);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.12);
}

.bg-tertiary-container {
  background: linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%);
}

.bento-full-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.icon-wrapper-tertiary {
  width: 88rpx;
  height: 88rpx;
  background-color: rgba(255, 255, 255, 0.5);
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.text-on-tertiary-container {
  color: #510d72;
}

.text-on-tertiary-container-80 {
  color: rgba(81, 13, 114, 0.8);
}

.text-tertiary {
  color: #7e3f9f;
}

.chevron-right {
  width: 40rpx;
  height: 40rpx;
  transform: rotate(90deg);
}

/* ========== Popular Tasks Section ========== */
.popular-tasks-section {
  padding: 32rpx 0;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 32rpx;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 36rpx;
  font-weight: 800;
  color: #2c2f32;
  letter-spacing: -0.01em;
}

.section-more {
  font-size: 22rpx;
  font-weight: 700;
  color: #005da4;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  /* iOS 风格动画 */
  transition: opacity 0.1s ease;
}

.section-more:active {
  opacity: 0.5;
}

/* [修复] 补充缺失的 .no-scrollbar 类定义（WXML 中使用但 CSS 中未定义） */
.no-scrollbar::-webkit-scrollbar {
  display: none;
}

.tasks-scroll {
  display: flex;
  flex-direction: column;
  padding: 0 32rpx;
  gap: 24rpx;
  box-sizing: border-box;
}

.task-card {
  flex-shrink: 0;
  width: 100%;
  padding: 32rpx;
  margin-bottom: 24rpx;
  background-color: #ffffff;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  box-sizing: border-box;
  transition: all 0.2s ease;
}

.task-card:active {
  transform: scale(0.985);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.12);
}

.task-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.task-tag {
  padding: 8rpx 20rpx;
  border-radius: 9999rpx;
  font-size: 20rpx;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}

.tag-urgent {
  background-color: rgba(179, 27, 37, 0.1);
  color: #b31b25;
}

.tag-new {
  background-color: rgba(79, 164, 255, 0.15);
  color: #005da4;
}

.tag-success {
  background-color: rgba(76, 175, 80, 0.1);
  color: #4CAF50;
}

.tag-warning {
  background-color: rgba(255, 152, 0, 0.1);
  color: #FF9800;
}

.task-price {
  font-size: 40rpx;
  font-weight: 800;
  color: #005da4;
}

.task-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #2c2f32;
  margin-bottom: 24rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.task-meta {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.task-meta-item {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.meta-icon {
  width: 28rpx;
  height: 28rpx;
}

.meta-text {
  font-size: 24rpx;
  color: #595c5e;
}

/* 空状态卡片 */
.empty-card {
  flex-shrink: 0;
  width: 100%;
  height: 280rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: #e5e8ec;
  border-radius: 32rpx;
  gap: 16rpx;
  box-sizing: border-box;
}

.empty-icon {
  font-size: 64rpx;
  color: #74777a;
}

.empty-text {
  font-size: 28rpx;
  color: #595c5e;
}

/* ========== Latest Posts Section ========== */
.latest-posts-section {
  padding: 32rpx;
}

.posts-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.post-item {
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding: 24rpx;
  background-color: #ffffff;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  transition: all 0.2s ease;
}

.post-item:active {
  transform: scale(0.985);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.12);
}

.post-icon-wrapper {
  width: 96rpx;
  height: 96rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.bg-secondary-container-30 {
  background-color: rgba(191, 209, 255, 0.3);
}

.bg-tertiary-container-30 {
  background-color: rgba(222, 152, 255, 0.3);
}

.bg-primary-container-20 {
  background-color: rgba(79, 164, 255, 0.2);
}

.post-icon {
  width: 48rpx;
  height: 48rpx;
  object-fit: contain;
}

.post-content {
  flex: 1;
  min-width: 0;
}

.post-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.post-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #2c2f32;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.post-price {
  font-size: 28rpx;
  font-weight: 700;
  color: #005da4;
  flex-shrink: 0;
}

.post-meta {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.meta-item {
  display: flex;
  align-items: center;
  /* [修复] 移除 gap，用 meta-icon-small 的 margin-right 控制，避免双重间距 */
  font-size: 20rpx;
  color: #595c5e;
}

.meta-icon-small {
  width: 20rpx;
  height: 20rpx;
  /* [修复] 保留 margin-right，移除父级 gap 避免双重间距 */
  margin-right: 6rpx;
  flex-shrink: 0;
}

/* ========== Floating Action Button ========== */
.fab-button {
  position: fixed;
  right: 32rpx;
  bottom: calc(120rpx + constant(safe-area-inset-bottom));
  bottom: calc(120rpx + env(safe-area-inset-bottom));
  width: 112rpx;
  height: 112rpx;
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(59, 130, 246, 0.35);
  z-index: 100;
  transition: all 0.2s ease;
}

.fab-button:active {
  transform: scale(0.97);
  box-shadow: 0 4rpx 12rpx rgba(59, 130, 246, 0.25);
}

.fab-icon {
  width: 56rpx;
  height: 56rpx;
  transform: rotate(45deg);
}

/* ========== 骨架屏 ========== */
.skeleton-card {
  background-color: #e5e8ec;
}

.skeleton {
  background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 8rpx;
}

.skeleton-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 24rpx;
  background: none;
  animation: none;
}

.skeleton-tag {
  width: 100rpx;
  height: 40rpx;
  border-radius: 20rpx;
}

.skeleton-price {
  width: 80rpx;
  height: 48rpx;
  border-radius: 8rpx;
}

.skeleton-title {
  width: 80%;
  height: 40rpx;
  margin-bottom: 24rpx;
}

.skeleton-info {
  width: 60%;
  height: 32rpx;
}

/* ========== Material Symbols 字体 ========== */
.material-symbols-outlined {
  font-family: 'Material Symbols Outlined';
  font-weight: normal;
  font-style: normal;
  font-size: 24rpx;
  line-height: 1;
  letter-spacing: normal;
  text-transform: none;
  display: inline-block;
  white-space: nowrap;
  word-wrap: normal;
  direction: ltr;
  -webkit-font-feature-settings: 'liga';
  -webkit-font-smoothing: antialiased;
  font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
}
