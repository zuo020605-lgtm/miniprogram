/* 页面容器 - 防止溢出 */
page { overflow-x: hidden; }
.page-container {
  min-height: 100vh;
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 100%);
  display: flex;
  flex-direction: column;
  overflow-x: hidden;
  padding-top: 0;
}



/* 标签导航 */
.tab-nav {
  display: flex;
  background: #fff;
  padding: 0 32rpx;
  border-bottom: 1rpx solid rgba(0,0,0,0.05);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  position: sticky;
  top: 0;
  z-index: 100;
}
.tab-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  padding: 32rpx 0;
  position: relative;
  transition: all 0.3s ease;
}
.tab-item:active {
  background-color: rgba(0, 0, 0, 0.02);
}
.tab-label {
  font-size: 30rpx;
  font-weight: 600;
  color: #6b7280;
  transition: all 0.3s ease;
  letter-spacing: 1rpx;
}
.tab-item.active .tab-label {
  color: #005da4;
  font-weight: 700;
}
.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 56rpx;
  height: 8rpx;
  background: #005da4;
  border-radius: 4rpx;
  box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.4);
  transition: all 0.3s ease;
}
.tab-count {
  min-width: 40rpx;
  height: 40rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
  color: #fff;
  font-size: 24rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 12rpx;
  box-shadow: 0 4rpx 12rpx rgba(251, 191, 36, 0.3);
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}
.tab-count.processing {
  background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
  box-shadow: 0 4rpx 12rpx rgba(96, 165, 250, 0.3);
}
.tab-count.done {
  background: linear-gradient(135deg, #4ade80 0%, #22c55e 100%);
  box-shadow: 0 4rpx 12rpx rgba(74, 222, 128, 0.3);
}

/* 骨架屏 */
.skeleton-container {
  padding: 24rpx;
}
.skeleton {
  background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 20rpx;
}
@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
.skeleton-card {
  height: 280rpx;
  margin-bottom: 24rpx;
}

/* 空状态 */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 48rpx;
  text-align: center;
}
.empty-visual {
  width: 120rpx;
  height: 120rpx;
  border-radius: 60rpx;
  background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 32rpx;
}
.empty-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #374151;
  margin-bottom: 12rpx;
}
.empty-desc {
  font-size: 26rpx;
  color: #9ca3af;
  margin-bottom: 40rpx;
}
.empty-action {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 24rpx 48rpx;
  background: #005da4;
  color: #fff;
  border: none;
  border-radius: 48rpx;
  font-size: 28rpx;
  font-weight: 600;
  box-shadow: 0 8rpx 32rpx rgba(59, 130, 246, 0.35);
  transition: transform 0.15s ease;
}
.empty-action:active {
  transform: scale(0.96);
}

/* 订单列表 */
.order-scroll {
  flex: 1;
  height: calc(100vh - 200rpx);
}
.order-list {
  padding: 24rpx;
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.swipe-cell {
  position: relative;
  overflow: hidden;
  border-radius: 24rpx;
}

.swipe-delete {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  width: 160rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #b31b25;
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 800;
}

/* 订单卡片 - 高级感设计 */
.order-card {
  position: relative;
  z-index: 2;
  background: #fff;
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
  transition: all 0.2s ease;
}

.swipe-content {
  transform: translateX(0);
}

.swipe-cell.is-open .swipe-content {
  transform: translateX(-160rpx);
}

.swipe-cell.is-open .swipe-content:active {
  transform: translateX(-160rpx);
}

.order-card:active {
  transform: scale(0.985);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
}

.swipe-cell.is-open .order-card:active {
  transform: translateX(-160rpx);
}

.card-header {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.service-badge {
  width: 72rpx;
  height: 72rpx;
  border-radius: 18rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.badge-blue { background: linear-gradient(135deg, rgba(59,130,246,0.15) 0%, rgba(59,130,246,0.08) 100%); }
.badge-green { background: linear-gradient(135deg, rgba(34,197,94,0.15) 0%, rgba(34,197,94,0.08) 100%); }
.badge-orange { background: linear-gradient(135deg, rgba(249,115,22,0.15) 0%, rgba(249,115,22,0.08) 100%); }
.badge-purple { background: linear-gradient(135deg, rgba(168,85,247,0.15) 0%, rgba(168,85,247,0.08) 100%); }

.card-title-wrap {
  flex: 1;
  min-width: 0;
}
.card-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #1a1a1a;
  display: block;
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.card-meta {
  display: flex;
  align-items: center;
  gap: 6rpx;
}
.meta-text {
  font-size: 22rpx;
  color: #9ca3af;
}

.card-price {
  display: flex;
  align-items: baseline;
  flex-shrink: 0;
}
.price-symbol {
  font-size: 24rpx;
  font-weight: 600;
  color: #3b82f6;
}
.price-value {
  font-size: 36rpx;
  font-weight: 800;
  color: #3b82f6;
  letter-spacing: -1rpx;
}

.card-body {
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  border-radius: 14rpx;
  padding: 18rpx 20rpx;
  margin-bottom: 20rpx;
}
.location-row {
  display: flex;
  align-items: center;
  gap: 10rpx;
}
.location-text {
  font-size: 24rpx;
  color: #6b7280;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-footer {
  display: flex;
  gap: 16rpx;
}
.card-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  height: 76rpx;
  border-radius: 16rpx;
  font-size: 26rpx;
  font-weight: 600;
  border: none;
  transition: all 0.15s ease;
}
.card-btn:active {
  transform: scale(0.97);
}
.card-btn.secondary {
  background: #fff;
  color: #3b82f6;
  border: 2rpx solid #e5e7eb;
}
.card-btn.danger {
  background: #fff;
  color: #ef4444;
  border: 2rpx solid #fecaca;
}
.card-btn.primary {
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
  color: #fff;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.3);
}

.load-more {
  padding: 32rpx;
  text-align: center;
}
.load-text {
  font-size: 24rpx;
  color: #9ca3af;
}

.safe-area {
  height: 120rpx;
}

/* 深色模式 */
@media (prefers-color-scheme: dark) {
  .page-container {
    background: linear-gradient(180deg, #0f172a 0%, #1e293b 100%);
  }
  .app-header {
    background: #1e293b;
    border-bottom-color: rgba(255,255,255,0.1);
  }
  .header-title { color: #f1f5f9; }
  .tab-nav {
    background: #1e293b;
    border-bottom-color: rgba(255,255,255,0.1);
  }
  .tab-label { color: #9ca3af; }
  .tab-item.active .tab-label { color: #f1f5f9; }
  .order-card { background: #1e293b; }
  .card-title { color: #f1f5f9; }
  .card-body { background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); }
  .location-text { color: #9ca3af; }
  .empty-visual { background: linear-gradient(135deg, #334155 0%, #1e293b 100%); }
}

/* Morandi my-posts and swipe pass. */
.page-container {
  background:
    radial-gradient(circle at 88% -4%, rgba(129, 148, 135, 0.12), rgba(129, 148, 135, 0) 30%),
    linear-gradient(180deg, #f3f5f6 0%, #e8edf0 100%) !important;
}

.tab-nav {
  gap: 10rpx;
  padding: 18rpx 20rpx !important;
  background: rgba(250, 252, 251, 0.92) !important;
  border-bottom: 1rpx solid rgba(105, 124, 134, 0.18) !important;
  box-shadow: 0 8rpx 24rpx rgba(58, 70, 76, 0.07) !important;
}

.tab-item {
  height: 68rpx;
  padding: 0 10rpx !important;
  border-radius: 20rpx;
}

.tab-label {
  color: #737e83 !important;
  font-size: 25rpx !important;
  letter-spacing: 0 !important;
}

.tab-item.active {
  background: rgba(96, 120, 137, 0.14) !important;
}

.tab-item.active .tab-label {
  color: #4f6675 !important;
}

.tab-item.active::after {
  display: none !important;
}

.tab-count,
.tab-count.processing,
.tab-count.done {
  background: rgba(96, 120, 137, 0.14) !important;
  color: #4f6675 !important;
  box-shadow: none !important;
  animation: none !important;
}

.order-list {
  padding: 24rpx !important;
  gap: 22rpx !important;
}

.swipe-cell {
  position: relative;
  overflow: hidden;
  border-radius: 28rpx !important;
  background: rgba(167, 127, 121, 0.14);
}

.swipe-delete {
  z-index: 1;
  width: 176rpx !important;
  border-radius: 0 28rpx 28rpx 0;
  background: rgba(167, 127, 121, 0.18) !important;
  color: #a06f6a !important;
}

.swipe-content {
  position: relative;
  z-index: 2;
  transform: translateX(0);
  transition: transform 180ms cubic-bezier(0.2, 0.8, 0.2, 1), box-shadow 180ms ease;
}

.swipe-cell.is-open .swipe-content,
.swipe-cell.is-open .order-card {
  transform: translateX(-176rpx) !important;
}

.order-card {
  padding: 28rpx !important;
  border-radius: 28rpx !important;
  background: rgba(253, 254, 253, 0.96) !important;
  border: 1rpx solid rgba(105, 124, 134, 0.18) !important;
  box-shadow: 0 16rpx 42rpx rgba(58, 70, 76, 0.1) !important;
}

.service-badge {
  background: rgba(96, 120, 137, 0.12) !important;
  color: #607889 !important;
}

.card-title {
  color: #2f3538 !important;
}

.meta-text,
.location-text,
.order-id {
  color: #737e83 !important;
}

.card-body {
  background: rgba(238, 243, 244, 0.96) !important;
  border: 1rpx solid rgba(105, 124, 134, 0.12) !important;
}

.card-footer {
  align-items: center !important;
}

.card-btn {
  min-width: 128rpx !important;
  height: 64rpx !important;
  border-radius: 20rpx !important;
}

.card-btn.primary {
  background: linear-gradient(135deg, #607889 0%, #4f6675 100%) !important;
}

.card-btn.secondary {
  background: rgba(238, 243, 244, 0.96) !important;
  color: #4f6675 !important;
  border: 1rpx solid rgba(105, 124, 134, 0.24) !important;
}

.card-btn.danger {
  background: rgba(167, 127, 121, 0.14) !important;
  color: #a06f6a !important;
  border: 1rpx solid rgba(160, 111, 106, 0.26) !important;
}
